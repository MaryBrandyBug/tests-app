export const selectUser = (state) => state?.user;
export const selectTest = (state) => state?.test;
export const selectLibrary = (state) => state?.library;
export const selectNewQuestions = (state) => state?.newQuestions;
